# CloneWars StackOverflow

    To start the app:
* from the root directory install dependencies and start the server
```
$ yarn
$ node app.js
```
* in another terminal change to client directory install dependencies and start:
```
$ cd client
$ yarn
$ yarn start
```
